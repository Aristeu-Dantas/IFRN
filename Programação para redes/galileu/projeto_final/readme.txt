*PROJETO FINAL*

Criando um chat entre clientes terminais (que irão rodar localmente em minha máquina) e usuários do telegram.
Eles irão se comunicar via broadcast. Eu já fiz um chat via broadcast onde os usuários terminais conversavam entre si.



        # #nome do cliente
        # if message.startswith('!u'):
        #     nome_cliente = message.split(' ', 1)[1]
        #     clients[cliente_socket] = nome_cliente
        #     clientsTelegram[nome_cliente] = cliente_socket

        # if message == '!l': #listar os clientes conectados
        #     cliente_socket.send(str(clients.values()).encode('utf-8'))
